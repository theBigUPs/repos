#include "tree.h"





int find_min_index(int arr[] ,int length)
{
	int minindex = 0;
	int min = 0;
	for (size_t i = 0; i < length; i++)
	{
		if (true)
		{

		}
	}
	return minindex;
}







int main()
{
	char letter[] = {'\n','s','t','a','i','p','e'};

	int freq[] = { 1,3,4,10,12,13,15 };
	int length = sizeof(freq) / sizeof(freq[1]);
	


	tree root[7];

	for (size_t i = 0; i < length; i++)
	{
		root[i].data = freq[i];
	}


	for (size_t i = 0; i < length; i++)
	{
		printf("%d\n",root[i].data);

	}

}